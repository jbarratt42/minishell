#include "minishell.h"

int main(int argc, char **argv, char **env)
{
    (void)argc;
    (void)argv;
    (void)env;

    // const char buffer[BUFFER_SIZE];

    // char *line = readline(buffer);

    // printf("%s", line);

    return (EXIT_SUCCESS);
}